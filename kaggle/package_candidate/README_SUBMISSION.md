# Submission Metadata

Status: historical pre-submission package snapshot of the package candidate.

This directory is a packaging variant and was not itself submitted to Kaggle.
The submitted entry is the notebook under `kaggle/notebook_candidate/`, which
succeeded with Kaggle public score `0.0` (see
`kaggle/submission_record/submission_record.json`). This package candidate
carries package-level MIT-0 metadata for its preflight file set.

Public scorecards remain public scorecard evidence only. They are not Kaggle
leaderboard evidence.
